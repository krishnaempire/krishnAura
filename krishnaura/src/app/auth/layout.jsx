
export const metadata = {
    title: "Kirshna Aura- Auth",
    description: "Auth Page",
};


export default function OrderLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
}
