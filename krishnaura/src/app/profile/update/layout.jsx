
export const metadata = {
    title: "Kirshna Aura- Update Profile",
    description: "Profile Update Page",
};


export default function OrderLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
}
